import { css } from 'lit'

export const settingsSelect = css`
    select {
        width: 100%;
        border: none;
        border-radius: var(--default-border-radius);
        padding: 10px;
        color: var(--primary-color);
        font-size: var(--global-font-size);
        background-color: var(--bg-color-3);
        border-right: 8px solid transparent;
    }
`
